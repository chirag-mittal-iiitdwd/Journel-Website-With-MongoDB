<%- include('partials/header') %>
    <h1>Home</h1>
    <p><%= homestarting %></p>
    <!-- <% for(var i=0;i<posts.length;i++) {%>
        <h3><%=posts[i].title%></h3>
        <p><%=posts[i].content%></p>
    <% }%> -->
    <% posts.forEach(function(it){ %>
        <h1><%= it.name %></h1>
        <p><%= it.content.substring(0,100) + "..." %><a href="/posts/<%= it.name %>">Read More</a></p>
    <% }) %>
<%- include('partials/footer') %>